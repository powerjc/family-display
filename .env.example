# Family Display Configuration

# API token for securing update endpoint
# Generate a secure token: openssl rand -hex 32
API_TOKEN=your-secure-token-here

# Port to run on (default 8080)
PORT=8080

# Timezone
TZ=America/Chicago
